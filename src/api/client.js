// Placeholder API layer. Metric Planner has no backend yet; this stub
// establishes the api/ folder for when planner endpoints land.
// Do not add live calls without a spec — ask the Founder first (see CLAUDE.md).
export const apiBase = import.meta.env.VITE_API_URL || ''
